# Home Yide (Fase 3) — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps com checkbox.

**Goal:** Home pública animada da Yide em `/site`, com movimentos GSAP (recriados), identidade Yide, seções dinâmicas (serviços/cases/depoimentos) e textos editáveis.

**Architecture:** `home_config` (jsonb) + service-role; server component `/site` busca dados e passa pro client animado `HomeYide` (GSAP). Painel de edição na Programação. Funções puras testadas.

**Tech Stack:** Next.js 16, Supabase, Tailwind, **gsap** (novo), next/font (Sora + IBM Plex Sans), vitest.

**Referência a reusar:** `@/lib/supabase/service-role`, `@/lib/seo/queries` (`getOrgPadrao`, `listServicosComPaginas`), `@/lib/seo/case-queries` (`listCasesPublicados`), `@/lib/seo/config` (`YIDE_NAP`), `@/lib/auth/session`, `@/lib/gerador-leads/queries`, `@/lib/blog/acesso`. Shell/estilo: `src/app/servicos/layout.tsx`.

**IMPORTANTE (direitos):** NÃO copiar HTML/imagens/marca do template de terceiros. Recriar apenas os MOVIMENTOS com conteúdo Yide.

---

## Task 1: Dependência gsap

- [ ] **Step 1:** `npm install gsap@^3.12.5`
- [ ] **Step 2:** confirmar em package.json e `npx tsc --noEmit`
- [ ] **Step 3:** Commit `chore: adiciona gsap`

---

## Task 2: Migration home_config

**Files:** Create `supabase/migrations/20260719060000_home_config.sql`

- [ ] **Step 1:**

```sql
-- Conteúdo editável da home institucional (1 linha por org).
create table if not exists public.home_config (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  dados jsonb not null default '{}',
  updated_at timestamptz not null default now(),
  unique (organization_id)
);
alter table public.home_config enable row level security;
-- Leitura/escrita via service-role (sem policy = nega direto ao cliente).
```

- [ ] **Step 2:** Commit `feat(home): migration home_config`

---

## Task 3: Config pura + defaults (teste)

**Files:** Create `src/lib/seo/home-config.ts`, Test `tests/unit/home-config.test.ts`

- [ ] **Step 1: Teste que falha**

```ts
import { describe, it, expect } from "vitest";
import { mergeHomeConfig, HOME_DEFAULTS } from "@/lib/seo/home-config";
describe("mergeHomeConfig", () => {
  it("row nula devolve defaults", () => {
    expect(mergeHomeConfig(null)).toEqual(HOME_DEFAULTS);
  });
  it("sobrepõe campos válidos e ignora stats malformados", () => {
    const r = mergeHomeConfig({ hero_titulo: "Oi", stats: [{ valor: "10", rotulo: "x" }, { valor: 1 }], clientes: ["A", 2] });
    expect(r.hero_titulo).toBe("Oi");
    expect(r.stats).toEqual([{ valor: "10", rotulo: "x" }]);
    expect(r.clientes).toEqual(["A"]);
  });
  it("string vazia no titulo cai no default", () => {
    expect(mergeHomeConfig({ hero_titulo: "" }).hero_titulo).toBe(HOME_DEFAULTS.hero_titulo);
  });
});
```

- [ ] **Step 2: Falhar** — `npx vitest run tests/unit/home-config.test.ts --exclude '**/.claude/**'`

- [ ] **Step 3: Implementar**

```ts
export interface Stat { valor: string; rotulo: string }
export interface HomeConfig {
  hero_titulo: string; hero_sub: string; stats: Stat[];
  sobre_titulo: string; sobre_texto: string; cta_titulo: string; clientes: string[];
}
export const HOME_DEFAULTS: HomeConfig = {
  hero_titulo: "Marketing e tecnologia que fazem sua empresa crescer",
  hero_sub: "Tráfego, sites, redes sociais e IA para negócios de Cuiabá, Salvador, Vila Velha e todo o Brasil.",
  stats: [
    { valor: "+100", rotulo: "clientes atendidos" },
    { valor: "4", rotulo: "praças no Brasil" },
    { valor: "5+", rotulo: "anos de estrada" },
    { valor: "24/7", rotulo: "acompanhamento" },
  ],
  sobre_titulo: "A Yide Digital",
  sobre_texto: "Somos uma agência de marketing e programação em Cuiabá que une performance, dados e tecnologia para gerar resultado de verdade.",
  cta_titulo: "Vamos crescer sua empresa?",
  clientes: [],
};

function isStat(v: unknown): v is Stat {
  return !!v && typeof v === "object" && typeof (v as Stat).valor === "string" && typeof (v as Stat).rotulo === "string";
}
export function mergeHomeConfig(row: Record<string, unknown> | null): HomeConfig {
  const r = row ?? {};
  const str = (k: keyof HomeConfig) => (typeof r[k] === "string" && (r[k] as string).trim() ? (r[k] as string) : (HOME_DEFAULTS[k] as string));
  const stats = Array.isArray(r.stats) ? (r.stats as unknown[]).filter(isStat) : HOME_DEFAULTS.stats;
  const clientes = Array.isArray(r.clientes) ? (r.clientes as unknown[]).filter((c): c is string => typeof c === "string" && c.trim() !== "") : HOME_DEFAULTS.clientes;
  return {
    hero_titulo: str("hero_titulo"), hero_sub: str("hero_sub"),
    stats: stats.length ? stats : HOME_DEFAULTS.stats,
    sobre_titulo: str("sobre_titulo"), sobre_texto: str("sobre_texto"),
    cta_titulo: str("cta_titulo"), clientes,
  };
}
```

- [ ] **Step 4: Passar** · **Step 5: Commit** `feat(home): config + defaults + testes`

---

## Task 4: Queries + actions

**Files:** Create `src/lib/seo/home-queries.ts`, `src/lib/seo/home-actions.ts`

- [ ] **Step 1: queries**

```ts
// src/lib/seo/home-queries.ts
import { createServiceRoleClient } from "@/lib/supabase/service-role";
import { mergeHomeConfig, type HomeConfig } from "./home-config";
export async function getHomeConfig(orgId: string): Promise<HomeConfig> {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const sb: any = createServiceRoleClient();
  try {
    const { data } = await sb.from("home_config").select("dados").eq("organization_id", orgId).maybeSingle();
    return mergeHomeConfig((data?.dados as Record<string, unknown>) ?? null);
  } catch { return mergeHomeConfig(null); }
}
```

- [ ] **Step 2: actions**

```ts
// src/lib/seo/home-actions.ts
"use server";
import { revalidatePath } from "next/cache";
import { createServiceRoleClient } from "@/lib/supabase/service-role";
import { requireAuth } from "@/lib/auth/session";
import { getOrganizationId } from "@/lib/gerador-leads/queries";
import { podeGerenciarBlog } from "@/lib/blog/acesso";
import type { Stat } from "./home-config";

interface Err { error: string }
function parseStats(raw: FormDataEntryValue | null): Stat[] {
  if (typeof raw !== "string" || !raw.trim()) return [];
  try {
    const arr = JSON.parse(raw);
    if (!Array.isArray(arr)) return [];
    return arr.filter((s) => s && typeof s.valor === "string" && typeof s.rotulo === "string")
      .map((s) => ({ valor: String(s.valor).trim(), rotulo: String(s.rotulo).trim() })).filter((s) => s.valor || s.rotulo);
  } catch { return []; }
}
function parseLista(raw: FormDataEntryValue | null): string[] {
  if (typeof raw !== "string" || !raw.trim()) return [];
  return raw.split("\n").map((l) => l.trim()).filter(Boolean);
}

export async function salvarHomeAction(formData: FormData): Promise<{ success: true } | Err> {
  const a = await requireAuth();
  if (!podeGerenciarBlog(a.role)) return { error: "Sem permissão" };
  const orgId = await getOrganizationId(a.id);
  if (!orgId) return { error: "Sem organização" };
  const dados = {
    hero_titulo: String(formData.get("hero_titulo") ?? "").trim(),
    hero_sub: String(formData.get("hero_sub") ?? "").trim(),
    stats: parseStats(formData.get("stats")),
    sobre_titulo: String(formData.get("sobre_titulo") ?? "").trim(),
    sobre_texto: String(formData.get("sobre_texto") ?? "").trim(),
    cta_titulo: String(formData.get("cta_titulo") ?? "").trim(),
    clientes: parseLista(formData.get("clientes")),
  };
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  const sb: any = createServiceRoleClient();
  const { error } = await sb.from("home_config").upsert({ organization_id: orgId, dados, updated_at: new Date().toISOString() }, { onConflict: "organization_id" });
  if (error) return { error: error.message };
  revalidatePath("/site"); revalidatePath("/programacao/seo/home");
  return { success: true };
}
```

- [ ] **Step 3: tsc + eslint** · **Step 4: Commit** `feat(home): queries + action de config`

---

## Task 5: Componentes animados (client, GSAP)

**Files:** Create `src/components/home/HomeYide.tsx` e subcomponentes em `src/components/home/`.

**Contexto de dados** (props do `HomeYide`):
```ts
interface HomeData {
  config: HomeConfig; // de @/lib/seo/home-config
  servicos: { id: string; nome: string; slug: string; descricao_base: string }[];
  cases: { slug: string; cliente: string; segmento: string; resultados: { rotulo: string; valor: string }[]; cover_image_url: string | null }[];
  depoimentos: { texto: string; autor: string; cliente: string }[];
}
```

Requisitos de MOTION (GSAP 3.12; `import gsap from "gsap"; import { ScrollTrigger } from "gsap/ScrollTrigger"; gsap.registerPlugin(ScrollTrigger)`), todos dentro de `useEffect`/`useLayoutEffect` com cleanup e **guardando `prefers-reduced-motion`** (se reduzido, não anima e mostra tudo visível):

- [ ] **Step 1: `Cursor.tsx`** — glow radial que segue o mouse. Atualiza CSS vars `--gx/--gy` no `mousemove` (via `gsap.quickTo` ou rAF), aplicadas num `div` fixo `radial-gradient(400px at var(--gx) var(--gy), rgba(45,230,230,.12), transparent)`. Só monta em pointer:fine (desktop). Sem dependência de layout.

- [ ] **Step 2: `Hero.tsx`** — fundo escuro (`bg-neutral-950`); título (`config.hero_titulo`) dividido em palavras em `<span>` e revelado com `gsap.from(words, { yPercent: 120, opacity: 0, stagger: .08, ease: "power3.out" })` no mount; subtítulo e CTA fade/slide; "color wipe": um overlay teal com `scaleX` 0→1→0 (clip) no load; logo Yide (`next/image`, `public/brand/logo-yide.png`) com leve float (`gsap.to(logo, { y: -10, repeat: -1, yoyo: true, duration: 2 })`). CTA WhatsApp `https://wa.me/5565981447380`.

- [ ] **Step 3: `Numeros.tsx`** — grid dos `config.stats`. Count-up: extrair número de `valor` (ex.: "+100" → 100; se não-numérico, não anima) e animar de 0 ao alvo com `ScrollTrigger` (`onEnter`), mantendo prefixo/sufixo. Barra/realce teal.

- [ ] **Step 4: `ServicosBento.tsx`** — bento grid dos `servicos` (cards de tamanhos variados). Entrada no scroll: `gsap.from(cards, { y: 40, opacity: 0, stagger: .1, scrollTrigger: { trigger, start: "top 80%" } })`. Cada card linka `/servicos/[slug]`, ícone (lucide por índice), nome, `descricao_base`. Hover com leve escala.

- [ ] **Step 5: `CasesDestaque.tsx`** — até 3 `cases` (some se vazio). Cards com cliente/segmento/1º resultado + link `/cases/[slug]`. Reveal no scroll (stagger). Reusa visual dos cards de `/cases`.

- [ ] **Step 6: `Depoimentos.tsx`** — grid/carrossel dos `depoimentos` (some se vazio). Card com aspas, texto, autor. Reveal no scroll.

- [ ] **Step 7: `Sobre.tsx`** — `config.sobre_titulo`/`sobre_texto`, com parallax leve num elemento decorativo (`ScrollTrigger scrub`).

- [ ] **Step 8: `Clientes.tsx`** — `config.clientes` (some se vazio) em faixa de chips; se muitos, marquee CSS.

- [ ] **Step 9: `CtaContato.tsx`** + **`FooterHome.tsx`** — CTA final (`config.cta_titulo` + WhatsApp), e rodapé com logo, links (Serviços/Cases/Blog), Instagram (`https://instagram.com/yide.digital`), LinkedIn, NAP (`YIDE_NAP`: telefone/e-mail/Cuiabá-MT), © ano.

- [ ] **Step 10: `HomeYide.tsx`** — compõe todas as seções na ordem do spec, provê o `Cursor`, e faz `ScrollTrigger.refresh()` no mount. Paleta teal/ciano; Sora nos títulos (`[font-family:var(--font-display)]`).

- [ ] **Step 11: tsc + eslint** `npx eslint src/components/home` · **Step 12: Commit** `feat(home): componentes animados (GSAP) da home Yide`

---

## Task 6: Layout + rota /site

**Files:** Create `src/app/site/layout.tsx`, `src/app/site/page.tsx`

- [ ] **Step 1: layout** — fontes Sora + IBM Plex Sans (variáveis `--font-display`/`--font-sans-blog`), wrapper `min-h-screen bg-[#faf9f7] text-neutral-900 [color-scheme:light] antialiased [font-family:var(--font-sans-blog)]`. (Molde: `src/app/servicos/layout.tsx`, sem o header/footer — a Home tem os seus próprios.)

- [ ] **Step 2: page** (`force-dynamic`):

```tsx
import type { Metadata } from "next";
import { getOrgPadrao, listServicosComPaginas } from "@/lib/seo/queries";
import { listCasesPublicados } from "@/lib/seo/case-queries";
import { getHomeConfig } from "@/lib/seo/home-queries";
import { HOME_DEFAULTS } from "@/lib/seo/home-config";
import { HomeYide } from "@/components/home/HomeYide";
export const dynamic = "force-dynamic";
export const metadata: Metadata = {
  title: "Yide Digital · Marketing, tecnologia e IA",
  description: HOME_DEFAULTS.hero_sub,
  alternates: { canonical: "/site" },
};
export default async function SitePage() {
  const orgId = await getOrgPadrao();
  const [config, servicos, cases] = await Promise.all([
    orgId ? getHomeConfig(orgId) : Promise.resolve(HOME_DEFAULTS),
    orgId ? listServicosComPaginas(orgId) : Promise.resolve([]),
    orgId ? listCasesPublicados(orgId) : Promise.resolve([]),
  ]);
  const servicosLimpos = servicos.map((s) => ({ id: s.id, nome: s.nome, slug: s.slug, descricao_base: s.descricao_base }));
  const casesTop = cases.slice(0, 3).map((c) => ({ slug: c.slug, cliente: c.cliente, segmento: c.segmento, resultados: c.resultados, cover_image_url: c.cover_image_url }));
  const depoimentos = cases.filter((c) => c.depoimento_texto).slice(0, 6)
    .map((c) => ({ texto: c.depoimento_texto, autor: c.depoimento_autor, cliente: c.cliente }));
  return <HomeYide data={{ config, servicos: servicosLimpos, cases: casesTop, depoimentos }} />;
}
```

- [ ] **Step 3: tsc + eslint** · **Step 4: Commit** `feat(home): rota pública /site`

---

## Task 7: Painel de edição da Home

**Files:** Create `src/components/seo/HomeConfigForm.tsx` (client), `src/app/(authed)/programacao/seo/home/page.tsx`

- [ ] **Step 1: `HomeConfigForm`** (client) — inputs: hero_titulo, hero_sub (textarea), stats (lista valor/rotulo add/remove → hidden `stats` JSON), sobre_titulo, sobre_texto (textarea), cta_titulo, clientes (textarea, 1 por linha). Botão Salvar chama `salvarHomeAction`; toast. Props: `inicial: HomeConfig`.
- [ ] **Step 2: page** (`force-dynamic`, gate `podeGerenciarBlog`): `getHomeConfig(orgId)` → `<HomeConfigForm inicial={...} />`, link "Ver home" → `/site`, voltar → `/programacao/seo`.
- [ ] **Step 3:** link "Home" no topo de `/programacao/seo/page.tsx`.
- [ ] **Step 4: tsc + eslint + `npm run build`** (rotas `/site`, `/programacao/seo/home`; reverter `public/sw.js` se mudar). · **Step 5: Commit** `feat(home): painel de edição da home`

---

## Task 8: PR + migration

- [ ] `git push -u origin feat/home-yide` → PR → CI verde → merge squash.
- [ ] Entregar SQL da `home_config`; instruir: rodar migration → abrir `/site` pra ver → editar em Programação → Serviços & SEO Local → Home.

---

## Self-Review (feito)
- **Cobertura:** gsap (T1); home_config (T2); defaults/merge puro+teste (T3); queries+action (T4); todos os movimentos + seções dinâmicas (T5); rota /site com dados dinâmicos (T6); painel editável (T7). ✓
- **Movimentos do template recriados** (cursor, hero reveal, color wipe, parallax, bento, count-up) — sem copiar HTML/imagens de terceiros. ✓
- **Consistência:** `HomeConfig`/`Stat` (T3) usados em T4/T5/T6/T7; `HOME_DEFAULTS` em T3/T6; dados dinâmicos de `listServicosComPaginas`/`listCasesPublicados` (Fases 1/2) em T6.
- **Reduced-motion:** exigido em T5 (acessibilidade).
- **Placeholders:** componentes animados têm specs de comportamento explícitos + snippets GSAP; backend com código completo.
